{
  "name": "SAFEco",
  "version": "1.0.0",
  "description": "The SAFEco company website.",
  "author": "Jason Veljanoski",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint",
    "deploy": "rm -r dist/* && nuxt generate && firebase deploy"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "nuxt": "^2.15.8",
    "vee-validate": "^2.1.5",
    "vue-carousel": "^0.16.1",
    "vue-scrollto": "^2.13.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0",
    "babel-eslint": "^8.2.1",
    "eslint": "^5.0.1",
    "eslint-loader": "^2.0.0",
    "eslint-plugin-vue": "^4.0.0"
  }
}
